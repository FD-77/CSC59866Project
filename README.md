For the pointindexing_template.cu file:
Try to set the limit to a lower number. In that way, you are able to assess if the outputs are actually correct (Since the default number for num_points is 100000. Way too many numbers).
